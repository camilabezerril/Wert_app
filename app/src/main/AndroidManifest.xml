<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="androidpro.com.wert">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wert">
        <activity android:name=".AlertasActivity" />
        <activity android:name=".CriarAcaoActivity" />
        <activity android:name=".InfosAcaoInativaActivity" />
        <activity android:name=".InfosAcaoActivity" />
        <activity android:name=".AcoesActivity" />
        <activity android:name=".LucroActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>